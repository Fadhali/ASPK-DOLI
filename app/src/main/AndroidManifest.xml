<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aspk1">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:screenOrientation="sensorPortrait"
        android:supportsRtl="true"
        android:theme="@style/Theme.ASPK1">
        <activity android:name=".ScanDeleteitemActivity"></activity>
        <activity android:name=".ScanSearchitemActivity" />
        <activity android:name=".ScanAdditemActivity" />
        <activity android:name=".viewInventoryActivity" />
        <activity android:name=".searchItemActivity" />
        <activity android:name=".deleteItemActivity" />
        <activity android:name=".addItemActivity" />
        <activity android:name=".LogIn" />
        <activity android:name=".SignUp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>